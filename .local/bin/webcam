#!/bin/sh

webcampid=$(xdotool search -name "webcam" getwindowpid)
[ -n "$webcampid" ] && kill -15 $webcampid && exit
mpv  --title=webcam /dev/video0
# mpv --no-cache --no-osc --no-input-default-bindings --input-conf=/dev/null --title=webcam $(ls /dev/video[0,2,4,6,8] | tail -n 1)
